#指定端口号
server:
  port: 9000

spring:
  application:
  #指定服务名称
    name: ribbon-consumer
eureka:
  client:
#是否获取其他服务地址列表
    fetch-registry: true
#  是否向注册中心注册
    register-with-eureka: true
    #默认注册中心URL
    service-url:
      defaultZone : http://replica1:5000/eureka/

