server:
  port: 5002  #应用程序端口
spring:
  application:
    name: config-server  #应用程序名
  cloud:
    config:
      server:
        svn:
          uri: svn://zlyfs.synology.me/CorrectionSYS/configData/ #svn地址
          username: zlyfs #svn用户名
          password: q1w2e3r4 #svn密码
        default-label: trunk   #trunk，配置文件的目录
  profiles:
    active: subversion  #表示这是采用的svn进行的spring cloud的配置


eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://replica1:5000/eureka/,http://replica2:5001/eureka/


